const config = {
    baseUrl: 'https://restful-booker.herokuapp.com',
    endpoints: {
        createBooking: '/booking',
        getBooking: '/booking/',
        login: '/auth'
    }
};

export default config;