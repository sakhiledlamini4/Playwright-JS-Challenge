apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    database: k6
    user: admin
    password: password
    isDefault: true
    editable: true
    jsonData:
      version: InfluxQL
